import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const publicPagesSlice = createSlice({
  name: "publicPages",
  initialState,
  reducers: {},
});

export default publicPagesSlice.reducer;
